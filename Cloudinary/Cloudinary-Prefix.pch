//
// Prefix header for all source files of the 'Cloudinary' target in the 'Cloudinary' project
//

#ifdef __OBJC__
    #import <Foundation/Foundation.h>
#endif

// Disables log messages when debugging is turned off
#ifndef NDEBUG

#define DebugLog(message, ...) NSLog(@"%s: " message, __PRETTY_FUNCTION__, ##__VA_ARGS__)

#else

#define DebugLog(message, ...)

#endif
